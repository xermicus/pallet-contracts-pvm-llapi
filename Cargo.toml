[package]
name = "pallet-contracts-pvm-llapi"
version = "0.1.0"
edition = "2021"

[dependencies]
inkwell = { version = "0.4", no-default-features = true, features = ["target-riscv", "no-libffi-linking", "llvm16-0"] }